Call db.create_all to create the tables you’ve defined so far:
db.create_all()
-------------------
Creating a test volunteer with some data and add it to the database:

test_volunteer = Volunteer(fname='test', lname='test', v_email='test@test.test', v_password='test', v_address='test')
db.session.add(test_volunteer)
db.session.commit()
-------------------
Instead of creating a new volunteer, query for one from the database:
>>> test1 = Volunteer.query.first()
>>> test1
<< Volunteer volunteer_id=1, fname=test, lname=test, v_email=test@test.test, v_password=test, v_address=test >>
>>> test1.fname
'test'
-------------------
To delete the database and create a new one.
dropdb project
createdb project
go to python -i model.py and run:
db.create_all()
**Should always do this after making changes to your schema (ex. adding tables, adding columns, changing the data types/names of columns)***
-------------------------
test_inst = Institution(inst_name='test_inst', inst_email='inst@test.test', inst_password='test_inst pass', inst_address='inst address test', )